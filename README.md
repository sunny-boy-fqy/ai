# AI CLI - 智能命令行助手

<div align="center">

![Version](https://img.shields.io/badge/version-0.2.0-blue.svg)
![Python](https://img.shields.io/badge/python-3.8+-green.svg)
![License](https://img.shields.io/badge/license-MIT-orange.svg)

**一个强大的 AI 命令行工具，支持多模式对话、MCP 插件自动发现安装、Leader-Worker 任务协作**

[快速开始](#-快速开始) • [功能特性](#-功能特性) • [详细文档](#-详细文档) • [示例](#-使用示例)

</div>

---

## 📑 目录

- [功能特性](#-功能特性)
- [快速开始](#-快速开始)
- [安装方式](#-安装方式)
- [基础功能](#-基础功能)
- [Leader-Worker 模式](#-leader-worker-模式)
- [多行输入支持](#-多行输入支持)
- [配置说明](#-配置说明)
- [内置插件](#-内置插件)
- [使用示例](#-使用示例)
- [故障排除](#-故障排除)
- [卸载](#-卸载)
- [依赖](#-依赖)
- [贡献](#-贡献)
- [许可证](#-许可证)

---

## ✨ 功能特性

### 🤖 多模式 AI 对话
- **即时问答**: 快速提问获取答案
- **对话模式**: 持续对话，保留上下文
- **历史记录**: 保存和恢复对话历史

### 🔌 MCP 插件生态
- **自动发现**: AI 自动识别能力差距
- **一键安装**: 无需手动配置，自动安装 MCP 插件
- **内置插件**: 内置 10+ 常用插件（数据库、浏览器、搜索等）

### 👔 Leader-Worker 任务协作
- **智能规划**: Leader AI 自动拆解复杂任务
- **自动执行**: Worker AI 使用插件全自动执行
- **进度监控**: 实时显示任务进度条
- **错误处理**: 自动重试，智能恢复
- **🆕 任务恢复**: `ai work --resume` 恢复上次未完成的任务
- **🆕 并行执行**: 多个独立任务可并行执行
- **🆕 智能上下文**: 自动压缩历史消息防止溢出

### 🆕 v0.2.0 新增功能
- **API 调用重试**: 失败时自动重试最多 3 次，指数退避
- **调试模式**: `ai --debug` 或 `ai work --debug` 启用详细日志
- **智能上下文压缩**: 超过 50 条消息时自动摘要压缩
- **任务恢复**: 程序中断后可恢复执行
- **并行任务**: Leader 可同时分配多个任务给 Worker
- **进度可视化**: 彩色进度条显示执行进度

### 💻 命令行友好
- **多行输入**: 支持代码块、续行符等多种多行输入方式
- **工作区管理**: 安全的文件访问控制
- **配置同步**: 支持 Git 仓库同步配置

---

## 🚀 快速开始

### 60 秒快速体验

```bash
# 1. 安装
curl -fsSL https://raw.githubusercontent.com/sunny-boy-fqy/ai/main/install.sh | bash
source ~/.bashrc

# 2. 创建供应商（首次使用）
ai new my-provider
# 按提示输入：Base URL、API Key、默认模型

# 3. 开始对话
ai ask "用 Python 写一个快速排序函数"

# 4. 进入对话模式
ai chat
```

### 5 分钟掌握核心功能

```bash
# 查看帮助
ai help

# 查看当前状态
ai status

# 安装插件
ai install memory

# 进入 Leader-Worker 模式
cd your-project
ai init --auto
ai work
```

---

## 📦 安装方式

### 方式一：一键安装（推荐）

```bash
# 下载并执行安装脚本
curl -fsSL https://raw.githubusercontent.com/sunny-boy-fqy/ai/main/install.sh | bash

# 重新加载配置
source ~/.bashrc

# 验证安装
ai version
```

安装脚本会自动：
- ✅ 克隆仓库到 `~/ai`
- ✅ 创建 Python 虚拟环境
- ✅ 安装 Node.js 环境到 `~/.config/ai/node_venv/`
- ✅ 安装必要依赖（openai, httpx, mcp）
- ✅ 添加 `ai` 命令到 PATH

### 方式二：手动安装

```bash
# 1. 克隆仓库
git clone https://github.com/sunny-boy-fqy/ai.git ~/ai
cd ~/ai

# 2. 创建 Python 虚拟环境
python3 -m venv ~/.config/ai/python_venv

# 3. 安装依赖
~/.config/ai/python_venv/bin/pip install openai httpx mcp

# 4. 添加到 PATH（选择一种）
# 方式 A: 添加 alias
echo 'alias ai="~/.config/ai/python_venv/bin/python ~/ai/ai.py"' >> ~/.bashrc
source ~/.bashrc

# 方式 B: 创建符号链接
sudo ln -s ~/.config/ai/python_venv/bin/python /usr/local/bin/aipython
sudo ln -s ~/ai/ai.py /usr/local/bin/ai
```

### 方式三：从源码更新

```bash
cd ~/ai
git pull origin main
```

---

## 📖 基础功能

### 对话命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `ai ask <问题>` | 即时问答 | `ai ask "写一个 Python 函数"` |
| `ai chat` | 进入对话模式 | `ai chat` |
| `ai history` | 查看历史记录 | `ai history` |
| `ai history load <编号>` | 加载历史对话 | `ai history load 1` |
| `ai history del <编号>` | 删除历史记录 | `ai history del 1` |

### 供应商管理

供应商是 AI 服务的提供商配置（如 OpenAI、智谱 AI 等）。

| 命令 | 说明 | 示例 |
|------|------|------|
| `ai new <名称>` | 创建供应商 | `ai new openai` |
| `ai use [名称]` | 切换供应商 | `ai use openai` |
| `ai model [名称]` | 切换模型 | `ai model gpt-4` |
| `ai list` | 列出所有供应商 | `ai list` |
| `ai del provider <名称>` | 删除供应商 | `ai del provider openai` |
| `ai status` | 显示当前状态 | `ai status` |

**创建供应商示例：**

```bash
$ ai new openai

配置供应商: openai
驱动类型:
  1. openai
  2. zhipuai
  3. groq
  4. dashscope
  5. anthropic
选择驱动编号 [1]: 1
Base URL [https://api.openai.com/v1/]: 
API Key: sk-xxxxx
默认模型 [gpt-3.5-turbo]: gpt-4o

✓ 供应商 'openai' 创建成功
切换到此供应商？[Y/n]: y
✓ 已切换到 'openai'
```

### 插件管理

AI CLI 支持 MCP（Model Context Protocol）插件，扩展 AI 的能力。

| 命令 | 说明 | 示例 |
|------|------|------|
| `ai search <关键词>` | 搜索插件 | `ai search database` |
| `ai install <名称>` | 安装插件 | `ai install memory` |
| `ai plugin` | 查看已安装插件 | `ai plugin` |
| `ai del plugin <名称>` | 卸载插件 | `ai del plugin memory` |

**搜索和安装示例：**

```bash
$ ai search database

=== 搜索: database ===
✓ postgres    [database] - PostgreSQL数据库
    需: POSTGRES_CONNECTION_STRING
✓ sqlite      [database] - SQLite数据库
✓ mysql       [database] - MySQL数据库

$ ai install sqlite

✓ 插件 'sqlite' 安装成功，加载 10 个工具
```

### 工作区管理

工作区是 AI 可以访问的目录范围，确保安全性。

| 命令 | 说明 | 示例 |
|------|------|------|
| `ai workspace` | 显示工作区 | `ai workspace` |
| `ai workspace <路径>` | 添加工作区 | `ai workspace ~/projects` |
| `ai workspace rm <编号>` | 移除工作区 | `ai workspace rm 1` |
| `ai workspace clear` | 清空工作区 | `ai workspace clear` |

### 任务管理

支持定时任务和周期任务的自动执行。

| 命令 | 说明 | 示例 |
|------|------|------|
| `ai task add <类型> <命令>` | 添加任务 | `ai task add once "echo hello"` |
| `ai task list` | 任务列表 | `ai task list` |
| `ai task del <ID>` | 删除任务 | `ai task del 20260213075412` |
| `ai task run <ID>` | 立即执行 | `ai task run 20260213075412` |
| `ai task start` | 启动守护进程 | `ai task start` |
| `ai task stop` | 停止守护进程 | `ai task stop` |

**任务类型：**
- `once` - 一次性任务
- `interval` - 周期任务（需指定秒数）
- `schedule` - 定时任务（cron 格式）

**示例：**

```bash
# 一次性任务
ai task add once "echo 'Hello World'"

# 周期任务（每 60 秒执行）
ai task add interval "date" 60

# 查看任务
ai task list

# 执行任务
ai task run 20260213075412
```

### 配置同步

支持将配置同步到 Git 仓库。

| 命令 | 说明 | 示例 |
|------|------|------|
| `ai sync <仓库>` | 从远程同步配置 | `ai sync git@github.com:user/config.git` |
| `ai update` | 更新程序 | `ai update` |
| `ai update <仓库>` | 上传配置到远程 | `ai update git@github.com:user/config.git` |

---

## 👔 Leader-Worker 模式

### 什么是 Leader-Worker 模式？

Leader-Worker 模式是一个强大的任务协作系统：

```
用户 ←→ Leader AI ←→ Worker AI
           ↓
      tasks.json
           ↓
      MCP 插件
```

- **Leader AI**: 任务总指挥，负责规划、分配、监控、协调
- **Worker AI**: 任务执行者，使用 MCP 插件自动执行具体任务

### 与普通 Chat 模式的区别

| 特性 | 普通 Chat 模式 | Leader-Worker 模式 |
|------|---------------|-------------------|
| 任务规划 | 一次性响应 | 持续规划调整 |
| 任务执行 | 单次对话 | 多步骤协作 |
| 进度显示 | 无 | 实时进度条 |
| 错误处理 | 需要用户干预 | 自动重试/重新规划 |
| 任务管理 | 无 | 完整的任务系统 |
| 用户交互 | 高频交互 | 低频干预 |

### 快速开始

#### 步骤 1：初始化项目

```bash
# 导航到你的项目目录
cd /path/to/your/project

# 初始化（交互式选择模型）
ai init

# 或使用自动模式（使用全局配置）
ai init --auto
```

初始化后创建的目录结构：

```
your-project/
├── .ai/
│   ├── leader_model.config  # Leader 模型配置
│   ├── worker_model.config  # Worker 模型配置
│   ├── workspace.config     # 工作区配置
│   ├── tasks.json          # 任务列表
│   └── tasks/              # 任务执行目录
```

#### 步骤 2：进入工作模式

```bash
ai work
```

#### 步骤 3：与 Leader AI 交互

```
=== Leader AI 会话 ===
项目目录: /path/to/your/project
Leader 模型: gpt-4o
Worker 模型: gpt-3.5-turbo

多行输入支持:
  - 以 \ 结尾继续输入下一行
  - 输入 ``` 开始多行块，再输入 ``` 结束
  - 或输入 """ 开始多行块，再输入 """ 结束

命令:
  - exit: 退出
  - status: 查看进度
  - clear: 清空已完成的任务

Leader> 创建一个简单的 Flask API 项目，包含用户增删改查功能

[Leader] 正在分析需求并规划任务...
已创建 5 个任务

任务进度
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
总计: 5 | 完成: 0 | 进行中: 1 | 待处理: 4 | 失败: 0
进度: [░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░] 0.0%

任务列表:
◐ task_001: 创建项目结构 (进行中)
   正在创建目录和基础文件...
○ task_002: 实现用户模型 (待处理)
○ task_003: 创建数据库连接 (待处理)
○ task_004: 实现 API 路由 (待处理)
○ task_005: 添加测试用例 (待处理)

[Worker 执行] filesystem__create_directory
[Worker 执行] filesystem__write_file
✓ task_001 完成

◐ task_002: 实现用户模型 (进行中)
...
```

### 工作流程详解

```
┌─────────────────────────────────────────────────────────────┐
│                     Leader-Worker 工作流                      │
└─────────────────────────────────────────────────────────────┘

1. 用户输入需求
   Leader> 创建一个用户认证系统
    
2. Leader AI 规划任务
   ├── 分析需求复杂度
   ├── 拆分为可执行子任务
   ├── 创建 tasks.json
   └── 设置任务依赖关系

3. Leader AI 分配任务
   ├── 检查任务依赖是否满足
   └── 将 ready 状态的任务分配给 Worker

4. Worker AI 执行任务
   ├── 使用 MCP 插件执行操作
   │   ├── 文件操作 (filesystem)
   │   ├── 代码编写
   │   ├── 命令执行 (shell)
   │   └── 测试验证
   ├── 更新任务状态
   └── 返回结果给 Leader

5. Leader AI 监控进度
   ├── 实时显示进度条
   ├── 处理 Worker 报告的错误
   ├── 必要时重新规划
   └── 向用户请求帮助

6. 所有任务完成
   └── 生成执行报告
```

### 配置文件详解

#### leader_model.config / worker_model.config

```json
{
  "provider": "openai",
  "model": "gpt-4o",
  "api_key": "sk-xxxxx",
  "base_url": "https://api.openai.com/v1/"
}
```

| 字段 | 说明 |
|------|------|
| `provider` | 供应商名称 |
| `model` | 模型名称 |
| `api_key` | API 密钥 |
| `base_url` | API 基础 URL |

#### tasks.json 结构

```json
{
  "project_name": "my-project",
  "created_at": "2024-01-15T10:30:00Z",
  "updated_at": "2024-01-15T11:00:00Z",
  "status": "running",
  "current_task": "task_002",
  "tasks": [
    {
      "id": "task_001",
      "title": "创建项目结构",
      "description": "创建基础目录和文件",
      "type": "code",
      "priority": 1,
      "status": "completed",
      "dependencies": [],
      "assigned_to": "worker",
      "created_at": "2024-01-15T10:30:00Z",
      "started_at": "2024-01-15T10:30:05Z",
      "completed_at": "2024-01-15T10:45:00Z",
      "files_to_modify": ["app.py", "requirements.txt"],
      "acceptance_criteria": ["目录结构正确", "文件创建成功"],
      "result_summary": "已创建 Flask 项目结构",
      "error_log": null,
      "notes": []
    }
  ],
  "statistics": {
    "total": 5,
    "pending": 3,
    "in_progress": 1,
    "completed": 1,
    "failed": 0
  }
}
```

#### 任务状态说明

| 状态 | 说明 | 颜色 |
|------|------|------|
| `pending` | 等待执行 | 黄色 ○ |
| `in_progress` | 正在执行 | 青色 ◐ |
| `completed` | 已完成 | 绿色 ● |
| `failed` | 执行失败 | 红色 ✗ |

#### 任务类型

| 类型 | 说明 | 示例 |
|------|------|------|
| `code` | 编写代码 | 实现函数、类、模块 |
| `doc` | 编写文档 | README、注释、API 文档 |
| `config` | 配置文件 | JSON、YAML、环境变量 |
| `test` | 测试相关 | 单元测试、集成测试 |
| `review` | 代码审查 | 检查、建议、优化 |
| `refactor` | 重构 | 代码优化、结构调整 |
| `fix` | 修复问题 | Bug 修复、错误处理 |

### 会话命令

在 Leader-Worker 会话中可用的命令：

| 命令 | 说明 | 示例 |
|------|------|------|
| `exit` 或 `quit` | 退出会话 | `exit` |
| `status` | 查看进度 | `status` |
| `clear` | 清空已完成任务 | `clear` |

### 内部接口

#### Leader 调用 Worker

```python
success, result = await leader.assign_task_to_worker(task)
```

#### Worker 报告错误给 Leader

```python
# Worker 返回
{
  "status": "failed",
  "error_message": "依赖包安装失败",
  "suggested_solution": "运行 pip install flask"
}

# Leader 处理
if needs_user_help:
    response = leader.request_user_help("需要安装 Flask 框架")
```

#### Leader 调用其他大模型

```python
# Leader 内部接口
response, tool_calls = await leader.model.call_async(
    prompt="分析这个需求",
    system_prompt="你是需求分析专家",
    tools=available_tools
)
```

### 示例场景

#### 场景 1：创建 Web 项目

```
Leader> 创建一个 Express.js 项目，包含用户认证和 REST API

[创建任务中...]
1. 初始化 Node.js 项目
2. 安装必要依赖
3. 创建项目结构
4. 实现用户模型
5. 创建认证中间件
6. 实现 API 路由
7. 添加错误处理
8. 编写测试用例
```

#### 场景 2：重构代码

```
Leader> 重构 utils.js，将重复代码提取为独立函数

[分析现有代码...]
1. 分析 utils.js 结构
2. 识别重复代码模式
3. 设计新的函数接口
4. 实现重构
5. 更新相关调用
6. 运行测试验证
```

#### 场景 3：添加功能

```
Leader> 为用户列表添加分页和搜索功能

[规划任务...]
1. 分析现有 API 结构
2. 设计分页参数
3. 实现搜索逻辑
4. 更新前端调用
5. 添加单元测试
```

### 进阶用法

#### 自定义任务

直接编辑 `.ai/tasks.json` 添加任务：

```json
{
  "id": "custom_001",
  "title": "自定义任务",
  "description": "手动添加的任务",
  "type": "code",
  "priority": 2,
  "status": "pending",
  "dependencies": [],
  "files_to_modify": [],
  "acceptance_criteria": []
}
```

#### 查看详细日志

```bash
# 查看任务文件
cat .ai/tasks.json

# 查看任务目录
ls -la .ai/tasks/

# 查看模型配置
cat .ai/leader_model.config
cat .ai/worker_model.config
```

---

## 📝 多行输入支持

在 `ai chat` 和 `ai work` 模式中，支持多种多行输入方式。

### 方式 1：反斜杠续行

以 `\` 结尾表示继续输入下一行：

```
Leader> 创建一个 Web 应用，\
... 包含用户认证功能，\
... REST API 接口，\
... 和数据库连接
```

### 方式 2：三引号块

输入 `"""` 开始多行块，再输入 `"""` 结束：

```
Leader> """
... 创建以下文件：
... 1. app.py - 主程序
... 2. models.py - 数据模型
... 3. routes.py - 路由定义
... 
... 要求：
... - 使用 Flask 框架
... - 遵循 PEP 8 规范
... - 包含完整的错误处理
... """
```

### 方式 3：三个反引号块（Markdown 风格）

输入 ``` 开始多行块，再输入 ``` 结束：

```
Leader> ```
... 实现以下 Python 类：
... 
... class UserManager:
...     def __init__(self, db):
...         self.db = db
...     
...     def create_user(self, username, email):
...         pass
...     
...     def get_user(self, user_id):
...         pass
... ```
```

### 注意事项

- 单行命令（如 `exit`、`status`、`clear`）直接执行，不进入多行模式
- 按 `Ctrl+C` 或 `Ctrl+D` 可取消当前输入
- 多行输入会保留所有换行符和格式

---

## ⚙️ 配置说明

### 配置目录结构

```
~/.config/ai/
├── python_venv/      # Python 虚拟环境
├── node_venv/        # Node.js 环境
│   └── bin/
│       ├── node
│       └── npx
├── mcp/              # MCP 插件目录
│   └── mcp.config    # 插件配置文件
├── config/           # 供应商配置目录
│   ├── <供应商名称>/
│   │   ├── api       # API 密钥列表（每行一个）
│   │   ├── url       # Base URL
│   │   └── model     # 模型列表（每行一个）
│   └── using.config  # 当前使用的供应商和模型
├── history/          # 对话历史目录
│   └── YYYYMMDD_HHMMSS.json
├── tasks.json        # 任务列表
├── base_path.config  # AI 安装目录路径
└── workspace.config  # 工作区配置
```

### 供应商配置文件

#### ~/.config/ai/config/<供应商>/url

```
https://api.openai.com/v1/
```

#### ~/.config/ai/config/<供应商>/api

```
sk-xxxxx
sk-yyyyy
```

#### ~/.config/ai/config/<供应商>/model

```
gpt-3.5-turbo
gpt-4
gpt-4o
```

#### ~/.config/ai/config/using.config

```
openai
gpt-4o
```

### MCP 插件配置

#### ~/.config/ai/mcp/mcp.config

```json
{
  "servers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user/projects"]
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    }
  }
}
```

---

## 🔌 内置插件

| 名称 | 功能说明 | 所需环境变量 |
|------|----------|--------------|
| **thinking** | 思维链推理，增强复杂问题分析 | - |
| **memory** | 知识图谱存储，长期记忆 | - |
| **filesystem** | 文件系统操作，读写文件 | - |
| **puppeteer** | 浏览器自动化，网页抓取 | - |
| **playwright** | Playwright 浏览器自动化 | - |
| **postgres** | PostgreSQL 数据库操作 | `POSTGRES_CONNECTION_STRING` |
| **sqlite** | SQLite 数据库操作 | - |
| **github** | GitHub API 集成 | `GITHUB_TOKEN` |
| **brave-search** | Brave 搜索引擎 | `BRAVE_API_KEY` |
| **slack** | Slack 消息发送 | `SLACK_BOT_TOKEN` |
| **mcp-shell-server** | Shell 命令执行 | - |

### 插件能力分类

```
📁 文件系统
  └── filesystem

🗄️ 数据库
  ├── postgres
  └── sqlite

🌐 浏览器/网页
  ├── puppeteer
  ├── playwright
  └── brave-search

🧠 AI/推理
  └── thinking

💾 存储
  └── memory

💬 通信
  └── slack

🔧 开发工具
  ├── github
  └── mcp-shell-server
```

---

## 📚 使用示例

### 示例 1：创建供应商并对话

```bash
# 创建 OpenAI 供应商
ai new openai
# 输入: https://api.openai.com/v1/, sk-xxxxx, gpt-4o

# 即时问答
ai ask "用 Python 写一个快速排序函数"

# 进入对话模式
ai chat
You > 帮我写一个 REST API
AI > 好的，我可以用 Flask 框架...
```

### 示例 2：插件管理

```bash
# 搜索数据库相关插件
ai search database

# 安装 SQLite 插件
ai install sqlite

# 查看已安装插件
ai plugin

# 卸载插件
ai del plugin sqlite
```

### 示例 3：Leader-Worker 项目开发

```bash
# 创建新项目
mkdir my-flask-api && cd my-flask-api

# 初始化
ai init --auto

# 进入工作模式
ai work

# 描述需求
Leader> 创建一个 Flask REST API，包含：
... 1. 用户注册和登录 (JWT 认证)
... 2. 用户的 CRUD 操作
... 3. SQLite 数据库
... 4. 单元测试
... 
# AI 自动规划并执行任务...

# 查看进度
Leader> status

# 退出
Leader> exit
```

### 示例 4：工作区配置

```bash
# 查看当前工作区
ai workspace

# 添加工作区
ai workspace ~/projects
ai workspace ~/Documents

# 移除工作区
ai workspace rm 1

# 清空工作区
ai workspace clear
```

### 示例 5：任务管理

```bash
# 添加一次性任务
ai task add once "python3 script.py"

# 添加周期任务（每 300 秒）
ai task add interval "python3 check_updates.py" 300

# 启动守护进程
ai task start

# 查看任务
ai task list

# 执行任务
ai task run 20260213075412

# 停止守护进程
ai task stop
```

### 示例 6：配置同步

```bash
# 从 Git 仓库同步配置
ai sync git@github.com:yourname/ai-config.git

# 上传配置到 Git 仓库
ai update git@github.com:yourname/ai-config.git

# 更新 AI 程序
ai update
```

---

## 🔧 故障排除

### 问题 1：安装失败

**症状**：安装脚本报错

**解决方案**：

```bash
# 检查依赖
python3 --version  # 需要 3.8+
node --version     # 需要 20+ (或让脚本自动安装)

# 手动安装
git clone https://github.com/sunny-boy-fqy/ai.git ~/ai
python3 -m venv ~/.config/ai/python_venv
~/.config/ai/python_venv/bin/pip install openai httpx mcp
```

### 问题 2：未配置供应商

**症状**：`ai ask` 或 `ai chat` 报错 "未配置供应商"

**解决方案**：

```bash
# 创建供应商
ai new my-provider

# 或查看是否有供应商
ai list
```

### 问题 3：Leader-Worker 初始化失败

**症状**：`ai init` 失败

**解决方案**：

```bash
# 检查全局配置
ai status

# 如果没有供应商，先创建
ai new my-provider

# 再初始化项目
ai init --auto
```

### 问题 4：Worker 执行失败

**症状**：任务一直失败

**解决方案**：

1. Leader AI 会自动尝试重新规划
2. 如果多次失败，Leader 会向用户请求帮助
3. 用户可以提供指导或使用 `clear` 清空已完成任务

### 问题 5：进度卡住

**症状**：任务进度不变

**解决方案**：

```bash
# 在 Leader-Worker 会话中
Leader> status    # 查看详细状态
Leader> clear     # 清空已完成任务

# 或手动重置
# 编辑 .ai/tasks.json，将所有 status 改为 "pending"
```

### 问题 6：插件安装失败

**症状**：`ai install xxx` 失败

**解决方案**：

```bash
# 检查是否需要环境变量
ai search xxx

# 设置环境变量
export GITHUB_TOKEN="your-token"
ai install github
```

### 问题 7：工作区未配置

**症状**：文件操作被拒绝

**解决方案**：

```bash
# 配置工作区
ai workspace ~/your-project

# 查看当前工作区
ai workspace
```

### 问题 8：模型输出异常 token

**症状**：输出包含 `<|xxx|>` 等奇怪标记

**解决方案**：

已自动处理！v0.1.1+ 版本会自动清理异常 token：
- `<|tool_calls_section_begin|>`
- `<|tool_call_begin|>`
- `functions.xxx:n`
- 等等

---

## 🗑️ 卸载

```bash
# 删除配置目录
rm -rf ~/.config/ai

# 删除程序目录
rm -rf ~/ai

# 从 shell 配置中删除 alias
# 编辑 ~/.bashrc，删除类似以下行：
# alias ai="..."
```

---

## 📦 依赖

### 必需依赖

- **Python** 3.8 或更高版本
- **Node.js** 20 或更高版本（自动安装到 `~/.config/ai/node_venv/`）

### Python 包（自动安装）

- `openai` - OpenAI API 客户端
- `httpx` - HTTP 客户端
- `mcp` - Model Context Protocol SDK

### 可选依赖

- `prompt_toolkit` - 更高级的输入处理（未来版本）

---

## 🤝 贡献

欢迎贡献代码、报告问题或提出建议！

### 如何贡献

1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request

### 代码风格

- Python: 遵循 PEP 8
- 提交信息: 使用简洁明了的描述

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

<div align="center">

**如果这个项目对你有帮助，请给一个 ⭐ Star！**

</div>
